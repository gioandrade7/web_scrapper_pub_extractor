# ─────────────────────────────────────────────────────────────
# Pipeline de Extração Semântica de Documentos Jurídicos
# Dependências dos dois módulos: text_extraction/ e semantic_seg/
# ─────────────────────────────────────────────────────────────

# ── Etapa 1: text_extraction/ ─────────────────────────────────
# PDF → Markdown, RTF → Texto, download de edições do DOU
pymupdf>=1.24.0            # Leitura e fusão de PDFs (fitz)
pymupdf4llm>=0.0.17        # Engine preferencial do pdf_extract.py (Markdown com
                           # detecção de coluna/tabela). Opcional: o import é
                           # protegido e há dois fallbacks em pymupdf puro.
httpx>=0.27.0              # Download das páginas em download_pdfs.py
striprtf>=0.0.26           # Conversão RTF → Texto

# ── Etapa 2: semantic_seg/ ────────────────────────────────────
# Segmentação semântica via LLM
openai>=1.30.0             # API OpenAI
python-dotenv>=1.0.0       # Carregamento do .env (OPENAI_API_KEY)
